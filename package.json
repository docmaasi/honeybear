{
  "name": "honeybear-katherine",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --port 3800",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview --port 3800",
    "audit": "node scripts/audit.mjs",
    "verify": "astro build && pagefind --site dist && node scripts/audit.mjs",
    "thumbs": "node scripts/fetch_thumbs.mjs",
    "links": "node scripts/check_links.mjs"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.6.0",
    "@fontsource-variable/bodoni-moda": "^5.3.0",
    "@fontsource-variable/outfit": "^5.3.0",
    "astro": "^7.3.1",
    "lite-youtube-embed": "^0.3.4",
    "pagefind": "^1.5.2",
    "sharp": "^0.34.4"
  }
}
